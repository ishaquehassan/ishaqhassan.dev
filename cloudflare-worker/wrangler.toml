name = "max-bot"
main = "src/worker.js"
compatibility_date = "2025-04-01"

# Workers AI binding (free tier: ~10k Neurons/day) — kept for legacy
[ai]
binding = "AI"

# Optional: KV for rate-limit counters (uncomment after creating namespace)
# [[kv_namespaces]]
# binding = "MAX_RL"
# id = "REPLACE_WITH_KV_NAMESPACE_ID"

[observability]
enabled = true

[vars]
ALLOWED_ORIGINS = "https://ishaqhassan.dev,https://www.ishaqhassan.dev,http://localhost:8000,http://127.0.0.1:8000"
